# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 7fd1305ef085afc3674e415b445b0310eea1570d91a1611daa56a0232736668a2c9f81c8f76ef9d07124979a27db0e37c5e73b7e67c180779ea6ef309a6de67b
  RAILS_SANDBOX_SPOTIFY_ClientID: <%= ENV["RAILS_SANDBOX_SPOTIFY_ClientID"] %>
  RAILS_SANDBOX_SPOTIFY_Client_Secret: <%= ENV["RAILS_SANDBOX_SPOTIFY_Client_Secret"] %>

test:
  secret_key_base: 7fd1305ef085afc3674e415b445b0310eea1570d91a1611daa56a0232736668a2c9f81c8f76ef9d07124979a27db0e37c5e73b7e67c180779ea6ef309a6de67b
  RAILS_SANDBOX_SPOTIFY_ClientID: <%= ENV["RAILS_SANDBOX_SPOTIFY_ClientID"] %>
  RAILS_SANDBOX_SPOTIFY_Client_Secret: <%= ENV["RAILS_SANDBOX_SPOTIFY_Client_Secret"] %>

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: Your_secret_key_base_goes_here
  RAILS_SANDBOX_SPOTIFY_ClientID: <%= ENV["RAILS_SANDBOX_SPOTIFY_ClientID"] %>
  RAILS_SANDBOX_SPOTIFY_Client_Secret: <%= ENV["RAILS_SANDBOX_SPOTIFY_Client_Secret"] %>
